const array = ['carro', 'casa', 'video game']
array[3] = 'cama'
array.push('jogos', 'comida', 'conforto')


let important = array[array.length -1]
console.log(array.length)
console.log(important)

let text = array.join(', ')

console.log(text)